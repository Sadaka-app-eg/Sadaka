// ==========================================
// مصفوفة التلاوات النادرة المظبوطة بالملي
// ==========================================
window.rareRecitations = [ 
  // تلاوات الشيخ محمد عباده (27 تلاوة)
  { name: "محمد عباده: ونفخ في الصور فصعق من في السماوات ومن في الأرض..... 🎙️", url: "audio/obada_1.mp3", desc: "", tag: "محمد عباده" },
  { name: "محمد عباده: تلاوة خاشعة 2 🎙️", url: "audio/obada_2.mp3", desc: "", tag: "محمد عباده" },
  { name: "محمد عباده: تلاوة خاشعة 3 🎙️", url: "audio/obada_3.mp3", desc: "", tag: "محمد عباده" },
  { name: "محمد عباده: تلاوة خاشعة 4 🎙️", url: "audio/obada_4.mp3", desc: "", tag: "محمد عباده" },
  { name: "محمد عباده: تلاوة خاشعة 5 🎙️", url: "audio/obada_5.mp3", desc: "", tag: "محمد عباده" },
  { name: "محمد عباده: تلاوة خاشعة 6 🎙️", url: "audio/obada_6.mp3", desc: "", tag: "محمد عباده" },
  { name: "محمد عباده: تلاوة خاشعة 7 🎙️", url: "audio/obada_7.mp3", desc: "", tag: "محمد عباده" },
  { name: "محمد عباده: تلاوة خاشعة 8 🎙️", url: "audio/obada_8.mp3", desc: "", tag: "محمد عباده" },
  { name: "محمد عباده: تلاوة خاشعة 9 🎙️", url: "audio/obada_9.mp3", desc: "", tag: "محمد عباده" },
  { name: "محمد عباده: تلاوة خاشعة 10 🎙️", url: "audio/obada_10.mp3", desc: "", tag: "محمد عباده" },
  { name: "محمد عباده: تلاوة خاشعة 11 🎙️", url: "audio/obada_11.mp3", desc: "", tag: "محمد عباده" },
  { name: "محمد عباده: تلاوة خاشعة 12 🎙️", url: "audio/obada_12.mp3", desc: "", tag: "محمد عباده" },
  { name: "محمد عباده: تلاوة خاشعة 13 🎙️", url: "audio/obada_13.mp3", desc: "", tag: "محمد عباده" },
  { name: "محمد عباده: تلاوة خاشعة 14 🎙️", url: "audio/obada_14.mp3", desc: "", tag: "محمد عباده" },
  { name: "محمد عباده: تلاوة خاشعة 15 🎙️", url: "audio/obada_15.mp3", desc: "", tag: "محمد عباده" },
  { name: "محمد عباده: تلاوة خاشعة 16 🎙️", url: "audio/obada_16.mp3", desc: "", tag: "محمد عباده" },
  { name: "محمد عباده: تلاوة خاشعة 17 🎙️", url: "audio/obada_17.mp3", desc: "", tag: "محمد عباده" },
  { name: "محمد عباده: تلاوة خاشعة 18 🎙️", url: "audio/obada_18.mp3", desc: "", tag: "محمد عباده" },
  { name: "محمد عباده: تلاوة خاشعة 19 🎙️", url: "audio/obada_19.mp3", desc: "", tag: "محمد عباده" },
  { name: "محمد عباده: تلاوة خاشعة 20 🎙️", url: "audio/obada_20.mp3", desc: "", tag: "محمد عباده" },
  { name: "محمد عباده: تلاوة خاشعة 21 🎙️", url: "audio/obada_21.mp3", desc: "", tag: "محمد عباده" },
  { name: "محمد عباده: تلاوة خاشعة 22 🎙️", url: "audio/obada_22.mp3", desc: "", tag: "محمد عباده" },
  { name: "محمد عباده: تلاوة خاشعة 23 🎙️", url: "audio/obada_23.mp3", desc: "", tag: "محمد عباده" },
  { name: "محمد عباده: تلاوة خاشعة 24 🎙️", url: "audio/obada_24.mp3", desc: "", tag: "محمد عباده" },
  { name: "محمد عباده: تلاوة خاشعة 25 🎙️", url: "audio/obada_25.mp3", desc: "", tag: "محمد عباده" },
  { name: "محمد عباده: تلاوة خاشعة 26 🎙️", url: "audio/obada_26.mp3", desc: "", tag: "محمد عباده" },
  { name: "محمد عباده: تلاوة خاشعة 27 🎙️", url: "audio/obada_27.mp3", desc: "", tag: "محمد عباده" },


// تلاوات الشيخ أحمد النفيس (5 تلاوات)
  { name: "أحمد النفيس: تلاوة خاشعة 1 🎙️", url: "audio/nafes_1.mp3", desc: "", tag: "أحمد النفيس" },
  { name: "أحمد النفيس: تلاوة خاشعة 2 🎙️", url: "audio/nafes_2.mp3", desc: "", tag: "أحمد النفيس" },
  { name: "أحمد النفيس: تلاوة خاشعة 3 🎙️", url: "audio/nafes_3.mp3", desc: "", tag: "أحمد النفيس" },
  { name: "أحمد النفيس: تلاوة خاشعة 4 🎙️", url: "audio/nafes_4.mp3", desc: "", tag: "أحمد النفيس" },
  { name: "أحمد النفيس: تلاوة خاشعة 5 🎙️", url: "audio/nafes_5.mp3", desc: "", tag: "أحمد النفيس" },

  // تلاوات القارئ حسن فهمي العدني (7 تلاوات)
  { name: "حسن فهمي العدني: تلاوة خاشعة 1 🎙️", url: "audio/hassan_1.mp3", desc: "", tag: "حسن فهمي" },
  { name: "حسن فهمي العدني: تلاوة خاشعة 2 🎙️", url: "audio/hassan_2.mp3", desc: "", tag: "حسن فهمي" },
  { name: "حسن فهمي العدني: تلاوة خاشعة 3 🎙️", url: "audio/hassan_3.mp3", desc: "", tag: "حسن فهمي" },
  { name: "حسن فهمي العدني: تلاوة خاشعة 4 🎙️", url: "audio/hassan_4.mp3", desc: "", tag: "حسن فهمي" },
  { name: "حسن فهمي العدني: تلاوة خاشعة 5 🎙️", url: "audio/hassan_5.mp3", desc: "", tag: "حسن فهمي" },
  { name: "حسن فهمي العدني: تلاوة خاشعة 6 🎙️", url: "audio/hassan_6.mp3", desc: "", tag: "حسن فهمي" },
  { name: "حسن فهمي العدني: تلاوة خاشعة 7 🎙️", url: "audio/hassan_7.mp3", desc: "", tag: "حسن فهمي" },


  
  // تلاوات الشيخ محمد صديق المنشاوي (9 تلاوات)
  { name: "المنشاوي: تلاوة خاشعة 1 🎙️", url: "audio/minshawi_1.mp3", desc: "", tag: "منشاوي" },
  { name: "المنشاوي: تلاوة خاشعة 2 🎙️", url: "audio/minshawi_2.mp3", desc: "", tag: "منشاوي" },
  { name: "المنشاوي: تلاوة خاشعة 3 🎙️", url: "audio/minshawi_3.mp3", desc: "", tag: "منشاوي" },
  { name: "المنشاوي: تلاوة خاشعة 4 🎙️", url: "audio/minshawi_4.mp3", desc: "", tag: "منشاوي" },
  { name: "المنشاوي: تلاوة خاشعة 5 🎙️", url: "audio/minshawi_5.mp3", desc: "", tag: "منشاوي" },
  { name: "المنشاوي: تلاوة خاشعة 6 🎙️", url: "audio/minshawi_6.mp3", desc: "", tag: "منشاوي" },
  { name: "المنشاوي: تلاوة خاشعة 7 🎙️", url: "audio/minshawi_7.mp3", desc: "", tag: "منشاوي" },
  { name: "المنشاوي: تلاوة خاشعة 8 🎙️", url: "audio/minshawi_8.mp3", desc: "", tag: "منشاوي" },
  { name: "المنشاوي: تلاوة خاشعة 9 🎙️", url: "audio/minshawi_9.mp3", desc: "", tag: "منشاوي" },

  // تلاوات الشيخ أحمد كاسب (6 تلاوات)
  { name: "أحمد كاسب: تلاوة خاشعة 1 🎙️", url: "audio/kaseb_1.mp3", desc: "", tag: "أحمد كاسب" },
  { name: "أحمد كاسب: تلاوة خاشعة 2 🎙️", url: "audio/kaseb_2.mp3", desc: "", tag: "أحمد كاسب" },
  { name: "أحمد كاسب: تلاوة خاشعة 3 🎙️", url: "audio/kaseb_3.mp3", desc: "", tag: "أحمد كاسب" },
  { name: "أحمد كاسب: تلاوة خاشعة 4 🎙️", url: "audio/kaseb_4.mp3", desc: "", tag: "أحمد كاسب" },
  { name: "أحمد كاسب: تلاوة خاشعة 5 🎙️", url: "audio/kaseb_5.mp3", desc: "", tag: "أحمد كاسب" },
  { name: "أحمد كاسب: تلاوة خاشعة 6 🎙️", url: "audio/kaseb_6.mp3", desc: "", tag: "أحمد كاسب" },

  // تلاوات الشيخ أحمد عبدالرازق نصر (7 تلاوات)
  { name: "أحمد عبدالرازق نصر: تلاوة خاشعة 1 🎙️", url: "audio/nasr_1.mp3", desc: "", tag: "أحمد عبدالرازق نصر" },
  { name: "أحمد عبدالرازق نصر: تلاوة خاشعة 2 🎙️", url: "audio/nasr_2.mp3", desc: "", tag: "أحمد عبدالرازق نصر" },
  { name: "أحمد عبدالرازق نصر: تلاوة خاشعة 3 🎙️", url: "audio/nasr_3.mp3", desc: "", tag: "أحمد عبدالرازق نصر" },
  { name: "أحمد عبدالرازق نصر: تلاوة خاشعة 4 🎙️", url: "audio/nasr_4.mp3", desc: "", tag: "أحمد عبدالرازق نصر" },
  { name: "أحمد عبدالرازق نصر: تلاوة خاشعة 5 🎙️", url: "audio/nasr_5.mp3", desc: "", tag: "أحمد عبدالرازق نصر" },
  { name: "أحمد عبدالرازق نصر: تلاوة خاشعة 6 🎙️", url: "audio/nast_6.mp3", desc: "", tag: "أحمد عبدالرازق نصر" },
  { name: "أحمد عبدالرازق نصر: تلاوة خاشعة 7 🎙️", url: "audio/nasr_7.mp3", desc: "", tag: "أحمد عبدالرازق نصر" },

  // تلاوات الشيخ أحمد بن طالب (5 تلاوات - مضافة حديثاً)
  { name: "أحمد بن طالب: تلاوة خاشعة 1 🎙️", url: "audio/taleb_1.mp3", desc: "", tag: "أحمد بن طالب" },
  { name: "أحمد بن طالب: تلاوة خاشعة 2 🎙️", url: "audio/taleb_2.mp3", desc: "", tag: "أحمد بن طالب" },
  { name: "أحمد بن طالب: تلاوة خاشعة 3 🎙️", url: "audio/taleb_3.mp3", desc: "", tag: "أحمد بن طالب" },
  { name: "أحمد بن طالب: تلاوة خاشعة 4 🎙️", url: "audio/taleb_4.mp3", desc: "", tag: "أحمد بن طالب" },
  { name: "أحمد بن طالب: تلاوة خاشعة 5 🎙️", url: "audio/taleb_5.mp3", desc: "", tag: "أحمد بن طالب" },

  // أدعية خاشعة (8 أدعية)
  { name: "ربي ما أرحمك دعاء ليلة 27 رمضان للشيخ حاتم فريد الواعر 🤲", url: "audio/doaa_1.mp3", desc: "", tag: "أدعية خاشعة" },
  { name: "بكاء وتضرع دعاء ليلة 27 رمضان للشيخ إسلام صبحي 🤲", url: "audio/doaa_2.mp3", desc: "", tag: "أدعية خاشعة" },
  { name: "دعاء الوتر 6 رمضان للشيخ محمد عبادة 🤲", url: "audio/doaa_3.mp3", desc: "", tag: "أدعية خاشعة" },
  { name: "يا فرجنا إذا أغلقت الأبواب للشيخ ياسر الدوسري 🤲", url: "audio/doaa_4.mp3", desc: "", tag: "أدعية خاشعة" },
  { name: "يا ملاذ الهاربين ويا أمان الخائفين ... دعاء ليلة 27 رمضان للقارئ محمد زاهر البارقي 🤲", url: "audio/doaa_5.mp3", desc: "", tag: "أدعية خاشعة" },
  { name: "يا حبيب التائبين . يا أنيس الخائفين للشيخ محمد عبادة 🤲", url: "audio/doaa_6.mp3", desc: "", tag: "أدعية خاشعة" },
  { name: "دعاء يريح النفس للشيخ منصور السالمي 🤲", url: "audio/doaa_7.mp3", desc: "", tag: "أدعية خاشعة" },
  { name: "دعاء يبكي الحجر للشيخ منصور السالمي 🤲", url: "audio/doaa_8.mp3", desc: "", tag: "أدعية خاشعة" }
];


// متغيرات التحكم بالصوت العام
// متغيرات التحكم بالصوت العام الموحد
if (!window.rareAudioPlayer) {
    window.rareAudioPlayer = new Audio();
}
window.currentRareUrl = '';
window.currentRareFilter = 'all';
window.currentRareGlobalId = null; // لربط الواجهة بالمشغل بدقة

// تايمر النوم (Sleep Timer)
window.sleepTimer = {
    active: false,
    endAt: 0,
    totalMinutes: 0,
    intervalId: null
};

window.formatTime = function (seconds) {
    if (!isFinite(seconds) || seconds < 0) seconds = 0;
    const m = Math.floor(seconds / 60);
    const s = Math.floor(seconds % 60);
    return `${m}:${s < 10 ? '0' : ''}${s}`;
};

window.getFilteredRareList = function () {
    return window.rareRecitations.filter(item => {
        if (window.currentRareFilter === 'all') return true;
        return item.tag === window.currentRareFilter;
    });
};

// محرك ربط الأحداث بالمشغل الموحد لمنع الرعشة والتضارب نهائياً
function ensureRareAudioEngine() {
    const player = window.rareAudioPlayer;
    
    player.onloadedmetadata = () => {
        const id = window.currentRareGlobalId;
        const totalEl = document.querySelector(`span[data-time-id="${id}"]`);
        if (totalEl && player.duration) {
            const activeProgressBar = document.querySelector(`input[data-progress-id="${id}"]`);
            if(activeProgressBar) activeProgressBar.value = (player.currentTime / player.duration) * 100;
            totalEl.textContent = `${window.formatTime(player.currentTime)} / ${window.formatTime(player.duration)}`;
        }
    };

    player.ontimeupdate = () => {
        const id = window.currentRareGlobalId;
        const activeProgressBar = document.querySelector(`input[data-progress-id="${id}"]`);
        const activeTimeLabel = document.querySelector(`span[data-time-id="${id}"]`);
        
        if (activeProgressBar && player.duration) {
            activeProgressBar.value = (player.currentTime / player.duration) * 100;
        }
        if (activeTimeLabel && player.duration) {
            activeTimeLabel.textContent = `${window.formatTime(player.currentTime)} / ${window.formatTime(player.duration)}`;
        }
    };

    player.onplay = () => {
        window.renderRareRecitations();
        if ('mediaSession' in navigator && window.currentRareUrl) {
            const currentTrack = window.rareRecitations.find(item => item.url === window.currentRareUrl);
            const trackName = currentTrack ? currentTrack.name.replace('🎙️', '').trim() : 'تلاوة خاشعة مؤثرة';
            const fullIconUrl = window.location.origin + window.location.pathname.replace(/[^\/]*$/, '') + 'icon.png';

            navigator.mediaSession.metadata = new MediaMetadata({
                title: trackName,
                artist: 'أثر',
                album: ' ',
                artwork: [
                    { src: fullIconUrl, sizes: '192x192', type: 'image/png' },
                    { src: fullIconUrl, sizes: '512x512', type: 'image/png' }
                ]
            });
        }
    };

    player.onpause = () => window.renderRareRecitations();

    player.onended = () => {
        if (window.sleepTimer.active && Date.now() < window.sleepTimer.endAt) {
            window.playNextRareTrack();
        } else {
            window.currentRareUrl = '';
            window.currentRareGlobalId = null;
            window.renderRareRecitations();
        }
    };
}

window.playNextRareTrack = function () {
    const list = window.getFilteredRareList();
    if (list.length === 0) return;

    let idx = list.findIndex(item => item.url === window.currentRareUrl);
    let nextIdx = (idx === -1) ? 0 : (idx + 1) % list.length;

    const nextTrack = list[nextIdx];
    const realGlobalId = window.rareRecitations.indexOf(nextTrack);
    
    window.currentRareGlobalId = realGlobalId;
    window.playRare(nextTrack.url);
};

window.startSleepTimer = function (minutes) {
    minutes = parseFloat(minutes);
    if (!minutes || minutes <= 0) return;

    window.sleepTimer.active = true;
    window.sleepTimer.totalMinutes = minutes;
    window.sleepTimer.endAt = Date.now() + minutes * 60 * 1000;

    if (!window.currentRareUrl || window.rareAudioPlayer.paused) {
        const list = window.getFilteredRareList();
        if (list.length > 0) {
            const startTrack = list[0];
            window.currentRareGlobalId = window.rareRecitations.indexOf(startTrack);
            window.playRare(startTrack.url);
        }
    }

    if (window.sleepTimer.intervalId) clearInterval(window.sleepTimer.intervalId);
    window.sleepTimer.intervalId = setInterval(window.tickSleepTimer, 1000);

    window.tickSleepTimer();
    window.renderSleepTimerBar();
};

window.cancelSleepTimer = function () {
    window.sleepTimer.active = false;
    window.sleepTimer.endAt = 0;
    if (window.sleepTimer.intervalId) {
        clearInterval(window.sleepTimer.intervalId);
        window.sleepTimer.intervalId = null;
    }
    window.renderSleepTimerBar();
};

window.tickSleepTimer = function () {
    if (!window.sleepTimer.active) return;
    const remainingMs = window.sleepTimer.endAt - Date.now();

    if (remainingMs <= 0) {
        window.rareAudioPlayer.pause();
        window.cancelSleepTimer();
        window.renderRareRecitations();
        return;
    }

    const remainingLabel = document.getElementById('sleepTimerRemaining');
    if (remainingLabel) {
        remainingLabel.textContent = window.formatTime(remainingMs / 1000);
    }
};

// دالة التشغيل الذكية الموحدة الخالية من الـ Loops والرعشة
window.playRare = function (url) {
    const player = window.rareAudioPlayer;
    ensureRareAudioEngine();

    // 🎯 تصحيح تلقائي برمجياً لغلطة اسم ملف جيت هاب الحالية دون الحاجة لإعادة الرفع
    let safeUrl = url;
    if (url === "audio/nasr_6.mp3") {
        safeUrl = "audio/nast_6.mp3";
    }

    if (window.currentRareUrl === url) {
        if (!player.paused) {
            player.pause();
        } else {
            player.play().catch(e => console.log("Play error:", e));
        }
    } else {
        player.src = safeUrl;
        window.currentRareUrl = url;
        player.play().catch(e => console.log("Play error:", e));
    }

    window.renderRareRecitations();
};

window.seekRare = function (element, url) {
    const player = window.rareAudioPlayer;
    const realIndex = window.rareRecitations.findIndex(item => item.url === url);
    if (window.currentRareGlobalId === realIndex && player.duration) {
        player.currentTime = (element.value / 100) * player.duration;
    } else {
        element.value = 0;
    }
};

window.filterRare = function (tag) {
    window.currentRareFilter = tag;
    const buttons = document.querySelectorAll('.sheikh-btn');
    buttons.forEach(btn => {
        btn.style.background = 'var(--card)';
        btn.style.color = 'var(--text)';
        btn.style.border = '1px solid var(--border)';
    });

    if (event && event.currentTarget) {
        const activeBtn = event.currentTarget;
        activeBtn.style.background = 'var(--gold)';
        activeBtn.style.color = '#111';
        activeBtn.style.border = 'none';
    }
    renderRareRecitations();
};

window.renderSleepTimerBar = function () {
    const bar = document.getElementById('sleepTimerBar');
    if (!bar) return;

    if (window.sleepTimer.active) {
        bar.innerHTML = `
          <div style="display:flex; align-items:center; justify-content:space-between; gap:10px; background:var(--card); border:1px solid var(--gold); border-radius:12px; padding:12px 15px; margin-bottom:15px; direction:rtl;">
            <div style="font-family:'Amiri',serif; color:var(--text); font-size:14px;">
              ⏱️ تايمر النوم شغال — هيقف بعد
              <span id="sleepTimerRemaining" style="color:var(--gold); font-weight:bold;">${window.formatTime((window.sleepTimer.endAt - Date.now()) / 1000)}</span>
            </div>
            <button onclick="window.cancelSleepTimer()" style="background:rgba(255,0,0,0.15); color:#ff6b6b; border:1px solid #ff6b6b; border-radius:8px; padding:6px 14px; cursor:pointer; font-family:'Amiri',serif;">إلغاء</button>
          </div>
        `;
    } else {
        bar.innerHTML = `
          <div style="display:flex; align-items:center; gap:10px; background:var(--card); border:1px solid var(--border); border-radius:12px; padding:12px 15px; margin-bottom:15px; direction:rtl;">
            <span style="font-family:'Amiri',serif; color:var(--text); font-size:14px;">⏱️ تايمر النوم (دقيقة):</span>
            <input id="sleepTimerInput" type="number" min="1" value="20" style="width:70px; padding:6px 8px; border-radius:8px; border:1px solid var(--border); background:transparent; color:var(--text); text-align:center;" />
            <button onclick="window.startSleepTimer(document.getElementById('sleepTimerInput').value)" style="background:var(--gold); color:#111; border:none; border-radius:8px; padding:6px 14px; cursor:pointer; font-family:'Amiri',serif; font-weight:bold;">بدء</button>
          </div>
        `;
    }
};

window.renderRareRecitations = function () {
    const container = document.getElementById('rareList');
    if (!container) return;

    window.renderSleepTimerBar();

    if (!window.rareRecitations || window.rareRecitations.length === 0) {
        container.innerHTML = '<div style="text-align:center; padding:20px; color:var(--text2);">⏳ جاري تحميل قائمة التلاوات...</div>';
        return;
    }

    const filteredList = window.getFilteredRareList();

    if (filteredList.length === 0) {
        container.innerHTML = '<div style="text-align:center; padding:20px; color:var(--text2); font-family:\'Amiri\',serif;">لا توجد تلاوات مرفوعة لهذا الشيخ بعد.</div>';
        return;
    }

    container.innerHTML = filteredList.map((item) => {
        const realIndex = window.rareRecitations.indexOf(item);
        const isCurrent = (window.currentRareUrl === item.url);
        const isPlaying = isCurrent && !window.rareAudioPlayer.paused;
        const icon = isPlaying ? '⏸' : '▶';

        const currentProgress = isCurrent && window.rareAudioPlayer.duration ? (window.rareAudioPlayer.currentTime / window.rareAudioPlayer.duration) * 100 : 0;
        const timeLabel = isCurrent && window.rareAudioPlayer.duration
          ? `${window.formatTime(window.rareAudioPlayer.currentTime)} / ${window.formatTime(window.rareAudioPlayer.duration)}`
          : '0:00 / --:--';

        return `
          <div style="background:var(--card); border-radius:15px; padding:15px; border:1px solid var(--border); margin-bottom:12px; display:flex; flex-direction:column; gap:10px;">
            <div style="display:flex; align-items:center; gap:15px; justify-content:space-between; width:100%;">
              <div style="display:flex; align-items:center; gap:10px;">
                <button onclick="window.currentRareGlobalId=${realIndex}; window.playRare('${item.url}')" style="background:var(--gold); border:none; width:42px; height:42px; border-radius:50%; color:#111; font-size:18px; cursor:pointer; display:flex; align-items:center; justify-content:center; transition:0.2s;">${icon}</button>
                <button id="rare_dl_${item.url.replace(/[^a-zA-Z0-9]/g,'_')}" onclick="window.downloadRareAudio('${item.url}')" style="background:rgba(255,255,255,0.1); border:1px solid var(--border); width:42px; height:42px; border-radius:50%; color:var(--text); font-size:16px; cursor:pointer; display:flex; align-items:center; justify-content:center; transition:0.2s;" title="تحميل للاستماع أوفلاين">📥</button>
              <button onclick="window.shareRareAudio('${item.name.replace(/'/g, "\\'")}', '${item.url}')" style="background:rgba(212,175,55,0.15); border:1px solid var(--gold); width:42px; height:42px; border-radius:50%; color:var(--gold); font-size:16px; cursor:pointer; display:flex; align-items:center; justify-content:center; transition:0.2s;" title="مشاركة التلاوة">🔗</button>
             
              
              </div>
              <div style="flex:1; text-align:right; direction:rtl;">
                <div style="font-weight:bold; color:var(--text); font-family:'Amiri',serif; font-size:15px; line-height:1.4;">${item.name}</div>
              </div>
            </div>
            <div style="width:100%; display:flex; flex-direction:column; gap:4px;">
              <input type="range"
                     data-progress-id="${realIndex}"
                     min="0" max="100"
                     value="${currentProgress}"
                     oninput="window.seekRare(this, '${item.url}')"
                     style="width:100%; accent-color:var(--gold); cursor:pointer; height:4px; border-radius:2px; background:rgba(255,255,255,0.2); outline:none;" />
              <span data-time-id="${realIndex}" style="font-size:12px; color:var(--text2); direction:ltr; text-align:left;">${timeLabel}</span>
            </div>
          </div>
        `;
    }).join('');

    setTimeout(markAlreadyDownloadedRareButtons, 100);
};

function rareBtnId(url) {
    return 'rare_dl_' + url.replace(/[^a-zA-Z0-9]/g, '_');
}

window.downloadRareAudio = function(url) {
    if (!navigator.serviceWorker || !navigator.serviceWorker.controller) {
        alert('⚠️ نظام التخزين لسه بيتجهز، جرب تاني بعد ثانية 🙏');
        return;
    }
    const btn = document.getElementById(rareBtnId(url));
    if (btn) {
        btn.disabled = true;
        btn.textContent = '⏳';
    }
    
    let safeUrl = url;
    if (url === "audio/nasr_6.mp3") safeUrl = "audio/nast_6.mp3";

    navigator.serviceWorker.controller.postMessage({
        type: 'CACHE_AUDIO_URL',
        url: safeUrl,
        label: 'rare_' + url
    });
};

navigator.serviceWorker.addEventListener('message', (event) => {
    const d = event.data;
    if (!d || !d.label || !d.label.startsWith('rare_')) return;
    const originalUrl = d.label.replace('rare_', '');
    const btn = document.getElementById(rareBtnId(originalUrl));
    if (!btn) return;

    if (d.type === 'AUDIO_CACHED') {
        btn.disabled = false;
        btn.textContent = '✅';
        btn.style.background = 'rgba(76,175,80,0.15)';
        btn.style.borderColor = '#4caf50';
        btn.style.color = '#4caf50';
    }
    if (d.type === 'AUDIO_CACHE_FAILED') {
        btn.disabled = false;
        btn.textContent = '⚠️';
    }
});

async function markAlreadyDownloadedRareButtons() {
    if (!('caches' in window)) return;
    try {
        const cache = await caches.open('athr-audio-cache-v1');
        for (const item of window.rareRecitations) {
            let safeUrl = item.url;
            if (item.url === "audio/nasr_6.mp3") safeUrl = "audio/nast_6.mp3";
            
            const match = await cache.match(safeUrl);
            const btn = document.getElementById(rareBtnId(item.url));
            if (match && btn) {
                btn.textContent = '✅';
                btn.style.background = 'rgba(76,175,80,0.15)';
                btn.style.borderColor = '#4caf50';
                btn.style.color = '#4caf50';
            }
        }
    } catch (e) {}
}

document.addEventListener('DOMContentLoaded', () => { renderRareRecitations(); });
// 📲 دالة مشاركة التلاوة الذكية (الملف المباشر)
window.shareRareAudio = async function(name, url) {
    let safeUrl = url === "audio/nasr_6.mp3" ? "audio/nast_6.mp3" : url;
    
    try {
        const response = await fetch(safeUrl);
        const blob = await response.blob();
        const file = new File([blob], `${name}.mp3`, { type: 'audio/mp3' });

        if (navigator.canShare && navigator.canShare({ files: [file] })) {
            await navigator.share({
                title: name,
                text: `تلاوة خاشعة: ${name} 🎙️`,
                files: [file]
            });
        } else {
            const fullAudioUrl = new URL(safeUrl, window.location.href).href;
            await navigator.share({ title: name, text: name, url: fullAudioUrl });
        }
    } catch (e) {
        window.downloadRareAudio(url);
    }
};
